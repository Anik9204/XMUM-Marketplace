CRITICAL RULES — READ BEFORE TOUCHING ANY FILE:
1. Before editing any file, list every other file that imports or uses it. Check all of those files before making changes.
2. Do NOT rewrite a file from scratch. Make surgical edits only — change the minimum number of lines needed to fix the issue.
3. Do NOT remove any existing function, prop, import, or JSX element unless I have explicitly told you to remove it.
4. After every change, mentally verify: "Does this break anything that was working before?" If yes, do not proceed — find a different approach.
5. If fixing an issue requires changing a shared component used by other pages, STOP and tell me what the impact will be before making the change. Ask for permission first.
6. Never change any CSS class, layout structure, z-index, or responsive breakpoint unless it is directly part of the fix.
7. Confirm the app builds with zero errors before finishing: pnpm --filter xmum-market run build

---

You are fixing 6 isolated bugs in the XMUM Market app. Fix them one at a time, in order. Do not skip any. Do not combine edits across files in a single step.

---

FIX 1 — Description sanitizer truncates to 1000 chars but UI allows 3000
File: artifacts/xmum-market/src/lib/sanitize.ts

Find this line:
  out.description = sanitizeText(out.description, 1000);

Change 1000 to 3000:
  out.description = sanitizeText(out.description, 3000);

That is the only change in this file. Nothing else.

---

FIX 2 — `now` is captured stale at render time in ProfilePage
File: artifacts/xmum-market/src/pages/ProfilePage.tsx

Find this line near the top of the ProfilePage component function body (outside any useEffect):
  const now = Date.now();

Wrap it in a useMemo so it is recomputed when the component re-renders due to data changes:
  const now = useMemo(() => Date.now(), [listings, refreshCounter]);

Make sure `useMemo` is already imported from React at the top of the file. If it is not in the import statement, add it. Check the existing import line — it likely reads:
  import { useState, useEffect, useRef } from "react";
Add useMemo to that same import, do not add a new import line.

---

FIX 3 — Unread message badge counts conversations instead of total unread messages
File: artifacts/xmum-market/src/lib/messaging.ts

Find the subscribeToUnreadCount function. Inside the onSnapshot callback, find this block:
  const total = snap.docs.filter((d) => {
    const count = d.data()?.unreadCount?.[uid] ?? 0;
    return typeof count === "number" && count > 0;
  }).length;
  callback(total);

Replace it with a proper sum:
  const total = snap.docs.reduce((sum, d) => {
    const count = d.data()?.unreadCount?.[uid] ?? 0;
    return sum + (typeof count === "number" ? count : 0);
  }, 0);
  callback(total);

That is the only change in this file.

---

FIX 4 — deleteListing timeout uses `resolve` instead of `reject`, silently swallowing delete failures
File: artifacts/xmum-market/src/lib/listings.ts

Find the Promise.race block inside deleteListing that looks like this:
  await Promise.race([
    deleteDoc(doc(db, "listings", listing.id)),
    new Promise<void>((resolve) => setTimeout(resolve, 6_000)),
  ]);

Change the timeout promise from resolve to reject:
  await Promise.race([
    deleteDoc(doc(db, "listings", listing.id)),
    new Promise<void>((_, reject) =>
      setTimeout(() => reject(new Error("timeout:delete-listing")), 6_000)
    ),
  ]);

That is the only change in this file.

---

FIX 5 — LoadingScreen has no fallback timeout if onFinish is never called
File: artifacts/xmum-market/src/App.tsx

Find the App function. It currently has:
  const [loading, setLoading] = useState(true);

Add a useEffect right after that line that adds a safety timeout:
  useEffect(() => {
    const t = setTimeout(() => setLoading(false), 5000);
    return () => clearTimeout(t);
  }, []);

Make sure useEffect is imported from React. Check the existing import — it likely already imports useState. Add useEffect to the same import line if it is not already there. Do not add a new import line.

---

FIX 6 — `shop-listing` is a dead value in ListingType that no code path uses
File: artifacts/xmum-market/src/lib/types.ts

Find the first line:
  export type ListingType = "buy-sell" | "lost-found" | "jobs" | "assistance" | "rental" | "shop-listing";

Remove "shop-listing" from the union:
  export type ListingType = "buy-sell" | "lost-found" | "jobs" | "assistance" | "rental";

After making this change, immediately run:
  pnpm --filter xmum-market run build

If the build fails with any TypeScript error mentioning "shop-listing", report the exact error to me before proceeding. Do not attempt to fix TypeScript errors yourself — just report them.

Also run:
  pnpm --filter xmum-admin run build

Report any errors.

---

AFTER ALL 6 FIXES:
1. Run pnpm --filter xmum-market run build — confirm zero errors.
2. Run pnpm --filter xmum-admin run build — confirm zero errors.
3. Commit with message: "fix: batch 1 — sanitizer limit, stale now, unread count, delete timeout, loading fallback, dead type"

⚠️ REMINDER FOR DEVELOPER: After Replit commits and Vercel deploys, manually test:
- Post a listing with a description longer than 1000 characters — it should now save the full text.
- Open Messages and check that the unread badge shows a number (it may be higher than before — that is correct, it now counts messages not conversations).
- No other visible changes expected.