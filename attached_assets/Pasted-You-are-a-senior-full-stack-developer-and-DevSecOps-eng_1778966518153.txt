You are a senior full-stack developer and DevSecOps engineer working on the
XMUM Marketplace monorepo. You are implementing ONE missing feature in the
ADMIN app only. Never touch artifacts/xmum-market/.
No changes to firestore.rules, firestore.indexes.json, or storage.rules.

CRITICAL RULES — same as all previous prompts. Apply them here.

═══════════════════════════════════════════════════
CONTEXT
═══════════════════════════════════════════════════

This feature was specified in a previous prompt but was never implemented.
The ShopsPage currently has only a Delete button per shop. It needs a
Suspend / Unsuspend toggle that sets isSuspended: true/false on the shop
document. The main student app reads isSuspended on the ShopPublicPage
to show a "shop is suspended" message instead of content.

Read the current ShopsPage.tsx in FULL before making any change.
Confirm: there is no isSuspended anywhere in the file, no handleSuspendToggle
function, no suspend button. That is the baseline to build from.

═══════════════════════════════════════════════════
STEP 1 — Add isSuspended to AdminShop interface
File: artifacts/xmum-admin/src/lib/types.ts
═══════════════════════════════════════════════════

Find the AdminShop interface. It currently ends with:
  inquiryCount?: number;
  createdAt: number;
}

Add isSuspended as an optional field. Change:
  inquiryCount?: number;
  createdAt: number;
}

To:
  inquiryCount?: number;
  createdAt: number;
  isSuspended?: boolean;
}

Do NOT change any other interface in types.ts.

═══════════════════════════════════════════════════
STEP 2 — Add new imports to ShopsPage.tsx
File: artifacts/xmum-admin/src/pages/ShopsPage.tsx
═══════════════════════════════════════════════════

2a — Add updateDoc and doc to the firebase import.

The current import from "../lib/firebase" is:
  import { getShops, deleteShop, writeAuditLog } from "../lib/firebase";

The functions updateDoc and doc live in firebase/firestore, not in the
local firebase.ts lib. Check the existing import from "firebase/firestore"
at the top — ShopsPage currently has NO import from "firebase/firestore"
at all. Add this new import line after the existing imports:

  import { updateDoc, doc } from "firebase/firestore";
  import { db } from "../lib/firebase";

Important: check if db is already imported from "../lib/firebase". Looking
at the current file, the import is:
  import { getShops, deleteShop, writeAuditLog } from "../lib/firebase";

db is NOT currently imported. Add it:
  import { getShops, deleteShop, writeAuditLog, db } from "../lib/firebase";

And separately add:
  import { updateDoc, doc } from "firebase/firestore";

2b — Add useAuth import.
The current file has: import { useAuth } from "../contexts/AuthContext";
Check if this is already present — it IS (line 4). Do not add it again.

2c — Add PauseCircle to the lucide-react import.

The current lucide-react import is:
  import { ExternalLink, Trash2, Search } from "lucide-react";

Change it to:
  import { ExternalLink, Trash2, Search, PauseCircle, PlayCircle } from "lucide-react";

═══════════════════════════════════════════════════
STEP 3 — Add suspending state and handleSuspendToggle function
File: artifacts/xmum-admin/src/pages/ShopsPage.tsx
═══════════════════════════════════════════════════

Inside ShopsPage(), after the existing state declaration:
  const [deleting, setDeleting] = useState<string | null>(null);

Add this new state declaration immediately after it:
  const [suspending, setSuspending] = useState<string | null>(null);

Then, after the entire handleDelete function (find its closing brace
followed by a blank line before the allCategories const), add the new
handleSuspendToggle function:

  async function handleSuspendToggle(shop: AdminShop) {
    const action = shop.isSuspended ? "unsuspend" : "suspend";
    if (!window.confirm(
      `${action === "suspend" ? "Suspend" : "Unsuspend"} shop "${shop.shopName}"?\n` +
      (action === "suspend"
        ? "The shop will be hidden from Campus Market."
        : "The shop will become visible again.")
    )) return;
    setSuspending(shop.id);
    try {
      await updateDoc(doc(db, "shops", shop.id), {
        isSuspended: !shop.isSuspended,
      });
      setShops((prev) =>
        prev.map((s) =>
          s.id === shop.id ? { ...s, isSuspended: !s.isSuspended } : s
        )
      );
      void writeAuditLog({
        actorUid:    adminUser?.uid   ?? "",
        actorEmail:  adminUser?.email ?? "",
        action:      shop.isSuspended ? "shop_unsuspended" : "shop_suspended",
        label:       `${shop.isSuspended ? "Unsuspended" : "Suspended"} shop "${shop.shopName}"`,
        targetId:    shop.id,
        targetType:  "shop",
        targetLabel: shop.shopName,
        createdAt:   Date.now(),
      });
    } catch (e) {
      console.error("[ShopsPage] suspend failed:", e);
      alert("Failed to update shop. Check Firestore permissions.");
    } finally {
      setSuspending(null);
    }
  }

NOTE: At the time writeAuditLog is called, shop.isSuspended still holds
the OLD value (before the toggle). The ternary uses that old value to
determine the correct action string — this is intentional and correct.

═══════════════════════════════════════════════════
STEP 4 — Add Suspended badge to shop name cell
File: artifacts/xmum-admin/src/pages/ShopsPage.tsx
═══════════════════════════════════════════════════

Find the JSX that renders the shop name in the table. It currently is:
  <div>
    <p className="font-semibold text-slate-800 dark:text-slate-200">
      {shop.shopName || "—"}
    </p>
    {shop.shopSlug && (
      <p className="text-[10px] text-slate-400">/{shop.shopSlug}</p>
    )}
  </div>

Add a Suspended badge immediately after the shop name <p>, inside the
same <div>:
  <div>
    <div className="flex items-center gap-1.5 flex-wrap">
      <p className="font-semibold text-slate-800 dark:text-slate-200">
        {shop.shopName || "—"}
      </p>
      {shop.isSuspended && (
        <span className="text-[10px] font-semibold bg-red-100 dark:bg-red-900/30
                         text-red-600 dark:text-red-400 px-2 py-0.5 rounded-full
                         flex-shrink-0">
          Suspended
        </span>
      )}
    </div>
    {shop.shopSlug && (
      <p className="text-[10px] text-slate-400">/{shop.shopSlug}</p>
    )}
  </div>

This wraps the name and badge in a flex row. The slug paragraph remains
below. Do NOT change anything else in that cell.

═══════════════════════════════════════════════════
STEP 5 — Add Suspend/Unsuspend button to the actions cell
File: artifacts/xmum-admin/src/pages/ShopsPage.tsx
═══════════════════════════════════════════════════

Find the actions <div> inside the last <td> of each shop row. It currently
contains the View <a> link (conditional on shopSlug) and the Delete button.

Add the Suspend/Unsuspend button BETWEEN the View link and the Delete
button. The complete actions div should become:

  <div className="flex items-center justify-center gap-1">
    {shop.shopSlug && (
      <a
        href={`${import.meta.env.VITE_MAIN_APP_URL ?? ""}/shop/${shop.shopSlug}`}
        target="_blank"
        rel="noopener noreferrer"
        title="View public shop page"
        className="flex items-center gap-1 text-[11px] text-blue-600
                   dark:text-blue-400 border border-blue-200
                   dark:border-blue-800 rounded-xl px-2.5 py-1.5
                   hover:bg-blue-50 dark:hover:bg-blue-900/20
                   transition-colors min-h-[34px]"
      >
        <ExternalLink className="w-3 h-3" /> View
      </a>
    )}
    <button
      onClick={() => handleSuspendToggle(shop)}
      disabled={suspending === shop.id}
      title={shop.isSuspended ? "Unsuspend shop" : "Suspend shop"}
      className={`flex items-center gap-1 text-[11px] rounded-xl px-2.5 py-1.5
                  border transition-colors min-h-[34px] disabled:opacity-50
                  ${shop.isSuspended
                    ? "border-green-200 dark:border-green-800 text-green-600 dark:text-green-400 hover:bg-green-50 dark:hover:bg-green-900/20"
                    : "border-amber-200 dark:border-amber-800 text-amber-600 dark:text-amber-400 hover:bg-amber-50 dark:hover:bg-amber-900/20"}`}
    >
      {suspending === shop.id
        ? "…"
        : shop.isSuspended
          ? <><PlayCircle  className="w-3 h-3" /> Unsuspend</>
          : <><PauseCircle className="w-3 h-3" /> Suspend</>}
    </button>
    <button
      onClick={() => handleDelete(shop)}
      disabled={deleting === shop.id}
      title="Delete shop"
      className="flex items-center gap-1 text-[11px] text-red-500
                 border border-red-200 dark:border-red-800 rounded-xl
                 px-2.5 py-1.5 hover:bg-red-50 dark:hover:bg-red-900/20
                 transition-colors min-h-[34px] disabled:opacity-50"
    >
      <Trash2 className="w-3 h-3" />
      {deleting === shop.id ? "…" : "Delete"}
    </button>
  </div>

NOTE: The View link's href was also fixed here (from the relative
/shop/${shop.shopSlug} to the absolute URL with VITE_MAIN_APP_URL).
This is because Prompt 1 already instructed that fix. If Prompt 1 was
run first, the href is already correct — keep the updated version.

═══════════════════════════════════════════════════
VERIFICATION
═══════════════════════════════════════════════════

1. Run: pnpm --filter xmum-admin run build — zero TypeScript errors.
   Pay attention to: isSuspended is optional (boolean | undefined) so
   the ternary must handle undefined as falsy — it already does naturally.
2. Verify: the Suspended badge appears only when isSuspended === true.
3. Verify: clicking Suspend on a shop calls handleSuspendToggle, which
   updates Firestore AND updates local state without a full reload.
4. Verify: the audit log entry uses the OLD isSuspended value (before
   toggle) to determine the correct action string. Double-check the
   ternary logic in the writeAuditLog call.
5. Zero changes to artifacts/xmum-market/ or any rules files.