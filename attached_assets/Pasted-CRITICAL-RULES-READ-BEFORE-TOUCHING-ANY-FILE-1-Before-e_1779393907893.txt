CRITICAL RULES — READ BEFORE TOUCHING ANY FILE:
1. Before editing any file, list every other file that imports or uses it. Check all of those files before making changes.
2. Do NOT rewrite a file from scratch. Make surgical edits only — change the minimum number of lines needed to fix the issue.
3. Do NOT remove any existing function, prop, import, or JSX element unless I have explicitly told you to remove it.
4. After every change, mentally verify: "Does this break anything that was working before?" If yes, do not proceed — find a different approach.
5. If fixing Issue X requires changing a shared component used by other pages, STOP and tell me what the impact will be before making the change. Ask for permission first.
6. Never change any CSS class, layout structure, z-index, or responsive breakpoint unless it is directly part of the fix.
7. Confirm the app builds with zero errors before finishing: pnpm --filter xmum-market run build

---

## ISSUE — "My Inquiries" section is a leftover of a deleted feature and must be safely removed

FILE: src/pages/ProfilePage.tsx

The "My Inquiries" section at the bottom of the profile page is a leftover from a previously removed feature. Remove it cleanly.

FIX — surgical changes to ProfilePage.tsx only:

1. Remove the entire "MY INQUIRIES" JSX block — it starts with the comment {/* ── MY INQUIRIES */} and ends with the closing </div> of that section.

2. Remove the two related state declarations:
   const [myInquiries, setMyInquiries] = useState<ShopInquiry[]>([]);
   const [inquiriesLoading, setInquiriesLoading] = useState(false);

3. In the useEffect that loads shops (the one calling getShopsByOwner and getShopsWhereEditor), remove these two lines inside it:
   setInquiriesLoading(true);
   getInquiriesForBuyer(user.uid).then(setMyInquiries).catch(() => {}).finally(() => setInquiriesLoading(false));

4. Check imports at the top of the file. After these removals, if ShopInquiry, InquiryStatus, and getInquiriesForBuyer are no longer used anywhere else in the file, remove them from their respective import lines. Do not remove Shop or any other type that is still used.

Do not touch any other file. Build and confirm zero errors.