You are continuing work on the XMUM Marketplace project. Read every file listed below carefully before writing a single line of code. Do NOT refactor anything not mentioned.

Files to read first:
- src/lib/types.ts
- src/lib/listings.ts
- src/lib/firebase.ts
- src/contexts/AuthContext.tsx
- src/components/Layout.tsx
- src/App.tsx
- src/lib/i18n.ts
- src/pages/ProfilePage.tsx

---

## FEATURE 7 — Post auto-deletion after 30 days with reminders

### Architecture decision (IMPORTANT — read before coding)
Firebase Cloud Functions (server-side cron) are NOT available in this Replit/Firebase project without a paid Blaze plan and separate deployment. Therefore, auto-deletion must be implemented CLIENT-SIDE using a "lazy expiry" pattern: expiry is checked and enforced when the user visits their Profile page and when listings are loaded. This is a standard pattern for student/hobby apps.

The approach:
1. Each listing already has `createdAt` (timestamp in milliseconds).
2. Define EXPIRY_MS = 30 days in milliseconds = `30 * 24 * 60 * 60 * 1000`.
3. Define REMINDER_MS = 23 days in milliseconds = `23 * 24 * 60 * 60 * 1000` (7 days before expiry).
4. When `ProfilePage` loads the user's listings, check each one: if `Date.now() - listing.createdAt >= EXPIRY_MS`, call `deleteListing(listing)` automatically and remove it from the list.
5. Show a dismissible notification banner on ProfilePage if any listing is within the reminder window (23–30 days old).
6. Show a toast notification when a listing is auto-deleted.

### Step A — Add expiry constants to listings.ts
At the top of `listings.ts`, after the `PAGE_SIZE` constant:
```ts
export const LISTING_EXPIRY_MS = 30 * 24 * 60 * 60 * 1000; // 30 days
export const LISTING_REMINDER_MS = 23 * 24 * 60 * 60 * 1000; // 23 days (7-day warning)
```

### Step B — Update ProfilePage.tsx to auto-delete expired listings and show reminder
In `ProfilePage.tsx`, after `getUserListings` returns data, run the expiry check:

```ts
import { deleteListing, getUserListings, markAsSold, LISTING_EXPIRY_MS, LISTING_REMINDER_MS } from "@/lib/listings";

// After fetching listings:
const now = Date.now();
const expired = data.filter(l => now - l.createdAt >= LISTING_EXPIRY_MS && l.status === "active");
const active = data.filter(l => !(now - l.createdAt >= LISTING_EXPIRY_MS && l.status === "active"));

// Auto-delete expired listings silently in background
if (expired.length > 0) {
  Promise.allSettled(expired.map(l => deleteListing(l))).then(() => {
    if (expired.length === 1) {
      setSuccessToast(`"${expired[0].title}" has been automatically removed after 30 days.`);
    } else {
      setSuccessToast(`${expired.length} listings have been automatically removed after 30 days.`);
    }
  });
}

listingsCache.current = active;
setListings(active);

// Check for listings in the reminder window (23–30 days old)
const expiringSoon = active.filter(
  l => l.status === "active" && 
  now - l.createdAt >= LISTING_REMINDER_MS && 
  now - l.createdAt < LISTING_EXPIRY_MS
);
if (expiringSoon.length > 0) {
  setExpiryReminders(expiringSoon);
}
```

Add state: `const [expiryReminders, setExpiryReminders] = useState<Listing[]>([]);`

### Step C — Show reminder banner in ProfilePage.tsx
Above the listings grid (after the tab bar), add a dismissible amber reminder banner when `expiryReminders.length > 0`:

```tsx
{expiryReminders.length > 0 && (
  <div className="mb-4 bg-amber-50 dark:bg-amber-900/20 border border-amber-200 dark:border-amber-700 rounded-2xl p-4 flex items-start gap-3">
    <Clock size={16} className="text-amber-600 dark:text-amber-400 shrink-0 mt-0.5" />
    <div className="flex-1 min-w-0">
      <p className="text-sm font-semibold text-amber-800 dark:text-amber-300">
        {t.listingExpiryReminderTitle}
      </p>
      <p className="text-xs text-amber-700 dark:text-amber-400 mt-0.5">
        {expiryReminders.length === 1
          ? `"${expiryReminders[0].title}" ${t.listingExpiryReminderBody}`
          : `${expiryReminders.length} ${t.listingExpiryReminderBodyMultiple}`}
      </p>
      <ul className="mt-1.5 space-y-0.5">
        {expiryReminders.map(l => {
          const daysLeft = Math.ceil((l.createdAt + LISTING_EXPIRY_MS - Date.now()) / (1000 * 60 * 60 * 24));
          return (
            <li key={l.id} className="text-xs text-amber-700 dark:text-amber-400 truncate">
              • {l.title} — {daysLeft} {t.daysLeft}
            </li>
          );
        })}
      </ul>
    </div>
    <button
      onClick={() => setExpiryReminders([])}
      className="text-amber-500 hover:text-amber-700 dark:hover:text-amber-300 shrink-0"
    >
      <X size={16} />
    </button>
  </div>
)}
```

Import `Clock` and `X` from lucide-react in ProfilePage.tsx (check if already imported — add only missing ones).

### Step D — Add translation keys to i18n.ts
Add to both EN and ZH:
- EN: `listingExpiryReminderTitle: "Some listings are expiring soon"`, `listingExpiryReminderBody: "will be automatically deleted in less than 7 days."`, `listingExpiryReminderBodyMultiple: "listings will be automatically deleted in less than 7 days."`, `daysLeft: "days left"`, `listingAutoDeleted: "listing was automatically removed after 30 days."`, `listingsAutoDeleted: "listings were automatically removed after 30 days."`
- ZH: `listingExpiryReminderTitle: "部分帖子即将到期"`, `listingExpiryReminderBody: "将在不到7天内自动删除。"`, `listingExpiryReminderBodyMultiple: "个帖子将在不到7天内自动删除。"`, `daysLeft: "天后到期"`, `listingAutoDeleted: "个帖子已在30天后自动删除。"`, `listingsAutoDeleted: "个帖子已在30天后自动删除。"`

---

## FEATURE 8 — Notification system (in-app, client-side)

### Architecture decision (IMPORTANT — read before coding)
A full push notification system requires Firebase Cloud Messaging and a service worker, which is complex and overkill for a student app. Instead, implement an IN-APP notification bell that shows a dropdown of recent app events that happened during the current session, plus persistent notifications stored in Firestore under `users/{uid}/notifications` subcollection.

### What notifications are generated:
1. Listing auto-deleted (generated in ProfilePage when expiry check runs)
2. Listing marked as sold (generated in ProfilePage/ListingDetailPage when markAsSold is called)
3. Welcome notification (generated once when user first logs in — check Firestore if a welcome notification already exists)

### Step A — Add notification types to types.ts
```ts
export interface AppNotification {
  id: string;
  type: "listing_deleted" | "listing_sold" | "welcome";
  title: string;
  body: string;
  createdAt: number;
  read: boolean;
  listingId?: string;
}
```

### Step B — Create src/lib/notifications.ts
```ts
import { collection, addDoc, getDocs, updateDoc, doc, query, orderBy, limit, serverTimestamp } from "firebase/firestore";
import { db } from "./firebase";
import { AppNotification } from "./types";

function toMillis(val: unknown): number {
  if (typeof val === "number") return val;
  if (val && typeof (val as any).toMillis === "function") return (val as any).toMillis();
  return Date.now();
}

export async function addNotification(
  uid: string,
  data: Omit<AppNotification, "id" | "createdAt" | "read">
): Promise<void> {
  try {
    await addDoc(collection(db, "users", uid, "notifications"), {
      ...data,
      createdAt: serverTimestamp(),
      read: false,
    });
  } catch {
    // Notifications are non-critical — silently fail
  }
}

export async function getNotifications(uid: string): Promise<AppNotification[]> {
  try {
    const q = query(
      collection(db, "users", uid, "notifications"),
      orderBy("createdAt", "desc"),
      limit(20)
    );
    const snap = await getDocs(q);
    return snap.docs.map(d => {
      const data = d.data();
      return { id: d.id, ...data, createdAt: toMillis(data.createdAt) } as AppNotification;
    });
  } catch {
    return [];
  }
}

export async function markNotificationsRead(uid: string, ids: string[]): Promise<void> {
  try {
    await Promise.allSettled(
      ids.map(id => updateDoc(doc(db, "users", uid, "notifications", id), { read: true }))
    );
  } catch {
    // Silent
  }
}
```

### Step C — Update Firestore rules to allow notifications subcollection
Add to the Firestore rules (this is the ONLY rules change allowed):
match /users/{userId}/notifications/{notifId} {
allow read, write: if request.auth != null && request.auth.uid == userId;
}
This goes INSIDE the existing `match /users/{userId}` block or alongside it within `match /databases/{database}/documents`. Add it as a sibling match block inside the databases match. Show the user exactly where to paste this in the Firebase console.

### Step D — Create NotificationBell component
Create `src/components/NotificationBell.tsx`:

```tsx
import { useState, useEffect, useRef } from "react";
import { Bell } from "lucide-react";
import { useAuth } from "@/contexts/AuthContext";
import { getNotifications, markNotificationsRead } from "@/lib/notifications";
import { AppNotification } from "@/lib/types";

export default function NotificationBell() {
  const { user } = useAuth();
  const [open, setOpen] = useState(false);
  const [notifications, setNotifications] = useState<AppNotification[]>([]);
  const [loading, setLoading] = useState(false);
  const dropdownRef = useRef<HTMLDivElement>(null);

  const unreadCount = notifications.filter(n => !n.read).length;

  useEffect(() => {
    if (!user) return;
    setLoading(true);
    getNotifications(user.uid)
      .then(setNotifications)
      .finally(() => setLoading(false));
  }, [user]);

  const handleOpen = async () => {
    setOpen(v => !v);
    if (!open && user) {
      const unreadIds = notifications.filter(n => !n.read).map(n => n.id);
      if (unreadIds.length > 0) {
        await markNotificationsRead(user.uid, unreadIds);
        setNotifications(prev => prev.map(n => ({ ...n, read: true })));
      }
    }
  };

  // Close on outside click
  useEffect(() => {
    const handler = (e: MouseEvent) => {
      if (dropdownRef.current && !dropdownRef.current.contains(e.target as Node)) {
        setOpen(false);
      }
    };
    document.addEventListener("mousedown", handler);
    return () => document.removeEventListener("mousedown", handler);
  }, []);

  if (!user) return null;

  return (
    <div className="relative" ref={dropdownRef}>
      <button
        onClick={handleOpen}
        className="p-2 min-h-[44px] min-w-[44px] flex items-center justify-center text-white/70 hover:text-white transition-colors rounded-lg hover:bg-white/10 relative"
        aria-label="Notifications"
      >
        <Bell size={17} />
        {unreadCount > 0 && (
          <span className="absolute top-1.5 right-1.5 w-4 h-4 bg-red-500 text-white text-[9px] font-bold rounded-full flex items-center justify-center">
            {unreadCount > 9 ? "9+" : unreadCount}
          </span>
        )}
      </button>

      {open && (
        <>
          <div className="fixed inset-0 z-40" onClick={() => setOpen(false)} />
          <div className="absolute right-0 top-11 w-80 bg-white dark:bg-slate-800 rounded-2xl shadow-xl border border-gray-100 dark:border-slate-700 z-50 overflow-hidden">
            <div className="px-4 py-3 border-b border-gray-100 dark:border-slate-700">
              <p className="text-sm font-bold text-gray-800 dark:text-slate-100">Notifications</p>
            </div>
            <div className="max-h-72 overflow-y-auto">
              {loading ? (
                <div className="p-4 text-center text-xs text-gray-400">Loading...</div>
              ) : notifications.length === 0 ? (
                <div className="p-6 text-center text-xs text-gray-400 dark:text-slate-500">
                  No notifications yet
                </div>
              ) : (
                notifications.map(n => (
                  <div
                    key={n.id}
                    className={`px-4 py-3 border-b border-gray-50 dark:border-slate-700/50 last:border-0 ${!n.read ? "bg-blue-50/50 dark:bg-blue-900/10" : ""}`}
                  >
                    <p className="text-xs font-semibold text-gray-800 dark:text-slate-100">{n.title}</p>
                    <p className="text-xs text-gray-500 dark:text-slate-400 mt-0.5">{n.body}</p>
                    <p className="text-[10px] text-gray-300 dark:text-slate-600 mt-1">
                      {new Date(n.createdAt).toLocaleDateString()}
                    </p>
                  </div>
                ))
              )}
            </div>
          </div>
        </>
      )}
    </div>
  );
}
```

### Step E — Add NotificationBell to Layout.tsx header
In `Layout.tsx`, import `NotificationBell` and render it in the header, between the language toggle and the desktop nav, visible only when user is logged in:
```tsx
import NotificationBell from "@/components/NotificationBell";

// In the header, after the language toggle button:
{user && <NotificationBell />}
```

### Step F — Trigger notifications from ProfilePage.tsx
In `ProfilePage.tsx`:
1. Import `addNotification` from `@/lib/notifications`
2. When auto-delete runs successfully, after the `setSuccessToast`, also call:
```ts
expired.forEach(l => {
  addNotification(user.uid, {
    type: "listing_deleted",
    title: "Listing Removed",
    body: `"${l.title}" was automatically removed after 30 days.`,
    listingId: l.id,
  });
});
```
3. In `handleMarkAsSold`, after the success update, also call:
```ts
addNotification(user.uid, {
  type: "listing_sold",
  title: listing.type === "lost-found" ? "Item Resolved" : "Item Marked as Sold",
  body: `"${listing.title}" has been marked as ${listing.type === "lost-found" ? "resolved" : "sold"}.`,
  listingId: listing.id,
});
```

---

## IMPORTANT CONSTRAINTS

1. The ONLY Firestore rules change allowed is adding the notifications subcollection rule shown above. Show the user exactly where to paste it in the Firebase Console — do NOT modify any other rules.
2. Do NOT change firebase.ts.
3. Do NOT add any new npm packages.
4. Notifications are non-critical — ALL notification Firestore operations must silently catch errors and never block the main app flow.
5. Do NOT implement push/browser notifications — only in-app dropdown.
6. Do NOT modify PostPage.tsx, SettingsPage.tsx, AuthModal.tsx, or ResetPasswordPage.tsx.
7. After all changes, list every file created or modified.