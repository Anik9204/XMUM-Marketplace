CRITICAL RULES — READ BEFORE TOUCHING ANY FILE:
1. Before editing any file, list every other file that imports or uses it. Check all of those files before making changes.
2. Do NOT rewrite a file from scratch. Make surgical edits only — change the minimum number of lines needed to fix the issue.
3. Do NOT remove any existing function, prop, import, or JSX element unless I have explicitly told you to remove it.
4. After every change, mentally verify: "Does this break anything that was working before?" If yes, do not proceed — find a different approach.
5. If fixing Issue X requires changing a shared component used by other pages, STOP and tell me what the impact will be before making the change. Ask for permission first.
6. Never change any CSS class, layout structure, z-index, or responsive breakpoint unless it is directly part of the fix.
7. Confirm the app builds with zero errors before finishing: pnpm --filter xmum-market run build

FILE TO EDIT: artifacts/xmum-market/src/components/AuthModal.tsx

FEATURE: Let students type just their student ID (e.g. "phy2502278") instead of their full email. If the value they typed in the email field does not contain "@", automatically append "@xmu.edu.my" before using it anywhere (validation, signIn, signUp, resetPasswordWithCheck). This applies to all three modes: signin, signup, and forgot.

EXACT CHANGES REQUIRED:

1. Add a helper function near the top of the component (after imports, before the `getPasswordStrength` function):

function resolveEmail(raw: string): string {
  const trimmed = raw.trim();
  return trimmed.includes("@") ? trimmed : `${trimmed}@xmu.edu.my`;
}

2. In the email <input> field JSX (the one with placeholder="student@xmu.edu.my"), change the type from "email" to "text" — this is necessary so that browsers don't reject "phy2502278" as an invalid email before we can append the domain. Keep all other attributes exactly as they are.

3. Directly below the email <input> closing tag (still inside the same <div>), add this hint line that shows the resolved email when the user has typed something without "@":

{email.trim() && !email.includes("@") && (
  <p className="text-[11px] text-blue-500 dark:text-blue-400 mt-1">
    → {email.trim()}@xmu.edu.my
  </p>
)}

4. In handleSubmit, at the very top of the try block (before any mode checks), add this one line to resolve the email:

const resolvedEmail = resolveEmail(email);

Then replace every usage of `email` that is passed to auth functions or validation with `resolvedEmail`. Specifically:
- `resetPasswordWithCheck(email)` → `resetPasswordWithCheck(resolvedEmail)`
- `isXmuEmail(email)` → `isXmuEmail(resolvedEmail)`
- `signUp(email, ...)` → `signUp(resolvedEmail, ...)`
- `checkRateLimit(\`login_${email}\`, ...)` → `checkRateLimit(\`login_${resolvedEmail}\`, ...)`
- `signIn(email, password)` → `signIn(resolvedEmail, password)`
- `setPendingEmail(email)` → `setPendingEmail(resolvedEmail)`

DO NOT change the `email` state variable itself or the onChange handler — the raw input stays in state, only the resolved version is used when submitting.

5. Do not change anything else — no layout, no CSS, no other logic.

After making changes, run: pnpm --filter xmum-market run build
Confirm zero TypeScript errors before finishing.