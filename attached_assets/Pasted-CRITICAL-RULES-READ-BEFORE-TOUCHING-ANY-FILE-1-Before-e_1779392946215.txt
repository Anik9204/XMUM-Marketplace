CRITICAL RULES — READ BEFORE TOUCHING ANY FILE:
1. Before editing any file, list every other file that imports or uses it. Check all of those files before making changes.
2. Do NOT rewrite a file from scratch. Make surgical edits only — change the minimum number of lines needed to fix the issue.
3. Do NOT remove any existing function, prop, import, or JSX element unless I have explicitly told you to remove it.
4. After every change, mentally verify: "Does this break anything that was working before?" If yes, do not proceed — find a different approach.
5. If fixing Issue X requires changing a shared component used by other pages, STOP and tell me what the impact will be before making the change. Ask for permission first.
6. Never change any CSS class, layout structure, z-index, or responsive breakpoint unless it is directly part of the fix.
7. Confirm the app builds with zero errors before finishing: pnpm --filter xmum-market run build

---

## ISSUE — Remove the Bump feature entirely from the codebase

FILES TO EDIT: src/pages/ProfilePage.tsx and src/lib/listings.ts

PART 1 — src/lib/listings.ts:
- Remove the BUMP_COOLDOWN_MS export constant.
- Remove the entire bumpListing() function.
- If the increment import from firebase/firestore is no longer used anywhere else in listings.ts after this removal, remove it from the import line. If it is still used elsewhere, leave it.

PART 2 — src/pages/ProfilePage.tsx:

In the OwnerListingRow component:
- Remove the onBump prop from the function parameters and its TypeScript interface.
- Remove the onCooldown variable (uses BUMP_COOLDOWN_MS).
- Remove the hoursSinceBump variable.
- Remove the green "↑ TOP" thumbnail badge JSX (the one that checks lastBumpedAt within 3 hours).
- Remove the entire Bump <button> from the action row (the one with the ArrowUp icon).

In the main ProfilePage component:
- Remove the handleBump function entirely.
- Remove the onBump={() => handleBump(l)} prop from every <OwnerListingRow> usage.
- Replace the expiry banner text "expires soon — bump it to refresh" with: expires soon — consider reposting

Clean up imports in ProfilePage.tsx:
- Remove ArrowUp from the lucide-react import line if no longer used.
- Remove bumpListing and BUMP_COOLDOWN_MS from the listings import line.

Do not touch any other file. Build and confirm zero errors.