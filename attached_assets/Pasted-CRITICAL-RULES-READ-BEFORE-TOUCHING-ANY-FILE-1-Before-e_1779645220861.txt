CRITICAL RULES — READ BEFORE TOUCHING ANY FILE:
1. Before editing any file, list every other file that imports or uses it. Check all of those files before making changes.
2. Do NOT rewrite a file from scratch. Make surgical edits only — change the minimum number of lines needed to fix the issue.
3. Do NOT remove any existing function, prop, import, or JSX element unless I have explicitly told you to remove it.
4. After every change, mentally verify: "Does this break anything that was working before?" If yes, do not proceed — find a different approach.
5. If fixing Issue X requires changing a shared component used by other pages, STOP and tell me what the impact will be before making the change. Ask for permission first.
6. Never change any CSS class, layout structure, z-index, or responsive breakpoint unless it is directly part of the fix.
7. Confirm the app builds with zero errors before finishing: pnpm --filter xmum-market run build

PROBLEM:
The Vercel serverless function at /api/moderate is not being detected. The build log 
confirms the build runs from artifacts/xmum-market/ — meaning Vercel's Root Directory 
is set to artifacts/xmum-market, so it only looks for api/*.ts inside that folder.

Currently api/moderate.ts lives at the monorepo root (XMUM-Marketplace/api/moderate.ts) 
where Vercel cannot see it.

FIX — do exactly these steps, nothing more:

STEP 1: Copy the file
- Copy the contents of XMUM-Marketplace/api/moderate.ts
- Create artifacts/xmum-market/api/moderate.ts with the same contents
- Delete XMUM-Marketplace/api/moderate.ts (the monorepo root copy)

STEP 2: Update artifacts/xmum-market/package.json
- Add "@vercel/node": "5.8.4" to devDependencies if it is not already there
- Do NOT change anything else in package.json

STEP 3: Update artifacts/xmum-market/vercel.json
- This file should already exist. Open it.
- Add a "functions" key if it does not exist:
  "functions": {
    "api/*.ts": {
      "runtime": "@vercel/node@5.8.4"
    }
  }
- Ensure the "rewrites" array has the /api route BEFORE the SPA catch-all:
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
- Do NOT change buildCommand, outputDirectory, or any other existing keys

STEP 4: Clean up the monorepo root vercel.json (XMUM-Marketplace/vercel.json)
- Remove the "functions" key from it (since the function no longer lives there)
- Remove the /api rewrite from it if present
- Do NOT touch anything else in that file

STEP 5: Verify aiModerate.ts still calls the correct path
- Open artifacts/xmum-market/src/lib/aiModerate.ts
- Confirm the fetch URL is "/api/moderate" (relative path) — do not change it

After all steps, run: pnpm --filter xmum-market run build
Confirm zero errors. Report back what changed.