CRITICAL RULES — READ BEFORE TOUCHING ANY FILE:
1. Before editing any file, list every other file that imports or uses it. Check all of those files before making changes.
2. Do NOT rewrite a file from scratch. Make surgical edits only — change the minimum number of lines needed to fix the issue.
3. Do NOT remove any existing function, prop, import, or JSX element unless I have explicitly told you to remove it.
4. After every change, mentally verify: "Does this break anything that was working before?" If yes, do not proceed.
5. Never change any CSS class, layout structure, z-index, or responsive breakpoint unless directly part of the fix.
6. Confirm the app builds with zero errors: pnpm --filter xmum-market run build

PROBLEM:
The Vercel serverless function approach (api/moderate.ts) cannot work with our
monorepo + custom buildCommand setup. Vercel's function scanner runs from the
repo root, not the Root Directory setting. We are switching to calling Gemini
directly from the client instead.

CHANGES TO MAKE — exactly these, nothing more:

STEP 1: Update artifacts/xmum-market/src/lib/aiModerate.ts
- Currently it does: fetch("/api/moderate", { method: "POST", body: ... })
- Change it to call the Gemini API directly using VITE_GEMINI_API_KEY
- Replace the entire fetch call and response handling with a direct Gemini call:

  const GEMINI_API_KEY = import.meta.env.VITE_GEMINI_API_KEY;
  const GEMINI_URL = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${GEMINI_API_KEY}`;

- The request body format sent to Gemini should be exactly the same JSON
  structure that api/moderate.ts was sending to Gemini internally.
- Open api/moderate.ts first and copy the exact prompt, request body structure,
  and response parsing logic into aiModerate.ts so the behaviour is identical.
- If VITE_GEMINI_API_KEY is missing or empty, log a warning and return
  { action: "safe", reason: "AI moderation unavailable" } — do not throw.

STEP 2: Add VITE_GEMINI_API_KEY to Vercel environment variables
- I will do this manually in the Vercel dashboard after you make the code change.
- You do NOT need to touch .env files or .env.example beyond adding the key name.
- Add VITE_GEMINI_API_KEY to artifacts/xmum-market/.env.example as a placeholder line:
  VITE_GEMINI_API_KEY=your_gemini_api_key_here

STEP 3: Remove the now-unused serverless function infrastructure
- Delete artifacts/xmum-market/api/moderate.ts
- In artifacts/xmum-market/vercel.json, remove the "functions" key entirely.
  Leave all other keys (rewrites, headers, buildCommand, outputDirectory) untouched.
- Remove @vercel/node from artifacts/xmum-market/package.json devDependencies.

STEP 4: Run pnpm install to update the lockfile after removing @vercel/node
  cd artifacts/xmum-market && pnpm install
  (or from monorepo root: pnpm install)

STEP 5: Build check
  pnpm --filter xmum-market run build
  Confirm zero errors. Report what changed.

IMPORTANT: Do not touch PostPage.tsx, ShopManagementPanel.tsx, aiFlag.ts, or
any other file. Only aiModerate.ts, vercel.json, package.json, .env.example,
and the deletion of api/moderate.ts.