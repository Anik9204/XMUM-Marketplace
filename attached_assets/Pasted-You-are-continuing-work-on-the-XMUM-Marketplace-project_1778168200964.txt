You are continuing work on the XMUM Marketplace project. Read every file listed below carefully before writing a single line of code. Fix only what is described.

Files to read first:
- src/pages/PostPage.tsx
- src/pages/EditListingPage.tsx
- src/pages/ListingDetailPage.tsx
- src/pages/SettingsPage.tsx
- src/lib/userProfile.ts
- src/lib/i18n.ts

---

## FIX 4 — At least one contact method required before posting

### In PostPage.tsx
In `handleSubmit`, after the content filter check and before the token refresh, add this validation:

```ts
// At least one contact method is required
const hasContact = whatsapp.trim() || wechat.trim() || teams.trim();
if (!hasContact) {
  setError(t.contactRequired);
  setLoading(false);
  return;
}
```

### In EditListingPage.tsx
Apply the exact same check in `handleSubmit`, in the same position (after content filter, before token refresh):
```ts
const hasContact = whatsapp.trim() || wechat.trim() || teams.trim();
if (!hasContact) {
  setError(t.contactRequired);
  setLoading(false);
  return;
}
```

### Add translation key to i18n.ts
Add to both EN and ZH:
- EN: `contactRequired: "Please provide at least one contact method (WhatsApp, WeChat, or MS Teams) before posting."`
- ZH: `contactRequired: "发布前请至少填写一种联系方式（WhatsApp、微信或MS Teams）。"`

---

## FIX 5 — Character limits on contact fields + WhatsApp country code hint + meet-up spot limit

### In PostPage.tsx — update the contact info section

Find the four input fields inside the contact info bordered box (whatsapp, wechat, teams, meetupSpot). Apply the following changes:

**WhatsApp field**: Change the label to include a country code reminder, and add `maxLength={20}` and a character counter:
```tsx
<div>
  <label className="block text-xs text-gray-500 dark:text-slate-400 mb-1">
    {t.whatsapp}
    <span className="text-amber-500 ml-1 font-medium">{t.whatsappCountryCodeHint}</span>
  </label>
  <input
    type="text"
    value={whatsapp}
    onChange={(e) => setWhatsapp(e.target.value.slice(0, 20))}
    placeholder="+60 12-345 6789"
    maxLength={20}
    className={inputCls}
  />
  <p className={`text-right text-[10px] mt-0.5 ${whatsapp.length >= 18 ? "text-amber-500" : "text-gray-400 dark:text-slate-500"}`}>
    {whatsapp.length}/20
  </p>
</div>
```

**WeChat field**: Add `maxLength={30}` and a character counter:
```tsx
<div>
  <label className="block text-xs text-gray-500 dark:text-slate-400 mb-1">{t.wechat}</label>
  <input
    type="text"
    value={wechat}
    onChange={(e) => setWechat(e.target.value.slice(0, 30))}
    placeholder="WeChat ID"
    maxLength={30}
    className={inputCls}
  />
  <p className={`text-right text-[10px] mt-0.5 ${wechat.length >= 27 ? "text-amber-500" : "text-gray-400 dark:text-slate-500"}`}>
    {wechat.length}/30
  </p>
</div>
```

**MS Teams field**: Already pre-filled with email — add `maxLength={60}` (max email length) silently, no visible counter needed.

**Meet-Up Spot field**: Add `maxLength={80}` and a character counter:
```tsx
<div>
  <label className="block text-xs text-gray-500 dark:text-slate-400 mb-1">{t.meetupSpot}</label>
  <input
    type="text"
    value={meetupSpot}
    onChange={(e) => setMeetupSpot(e.target.value.slice(0, 80))}
    placeholder={t.meetupSpotPlaceholder}
    maxLength={80}
    className={inputCls}
  />
  <p className={`text-right text-[10px] mt-0.5 ${meetupSpot.length >= 70 ? "text-amber-500" : "text-gray-400 dark:text-slate-500"}`}>
    {meetupSpot.length}/80
  </p>
</div>
```

Also add a note at the top of the contact info box (below the `<p className="text-sm font-semibold ...">Contact Info</p>` line):
```tsx
<p className="text-xs text-amber-600 dark:text-amber-400 bg-amber-50 dark:bg-amber-900/20 rounded-lg px-2.5 py-1.5">
  {t.contactRequired} {t.whatsappCountryCodeHint}
</p>
```

Wait — that's too verbose. Instead just add a single clean note:
```tsx
<p className="text-xs text-gray-400 dark:text-slate-500">
  {t.contactAtLeastOne}
</p>
```

### In EditListingPage.tsx — apply the exact same contact field changes
Apply all four field changes (maxLength, onChange with slice, counters) identically to the contact info box in EditListingPage.tsx. Copy the exact same JSX for all four fields from PostPage.

### Add translation keys to i18n.ts
Add to both EN and ZH:
- EN: `whatsappCountryCodeHint: "Include country code (e.g. +60 for Malaysia)"`, `contactAtLeastOne: "At least one contact method is required. Include country code for WhatsApp (e.g. +60)."`
- ZH: `whatsappCountryCodeHint: "请包含国家代码（如马来西亚为 +60）"`, `contactAtLeastOne: "请至少填写一种联系方式。WhatsApp 请包含国家代码（如 +60）。"`

---

## FIX 6 — Show poster's email on listing detail page, remove "Show Email" from privacy settings

### Part A — Show email on ListingDetailPage.tsx

In the seller info box (the `<div className="flex items-center gap-3 bg-gray-50 ...">` block), add the poster's email below their display name, always visible:

```tsx
<div className="flex items-center gap-3 bg-gray-50 dark:bg-slate-800 rounded-xl p-3">
  {sellerProfile?.avatarUrl ? (
    <img src={sellerProfile.avatarUrl} alt="seller" className="w-9 h-9 rounded-full object-cover border border-gray-200 dark:border-slate-600" />
  ) : (
    <div className="w-9 h-9 rounded-full bg-[#003366] flex items-center justify-center text-white font-semibold text-sm">
      {listing.userEmail[0].toUpperCase()}
    </div>
  )}
  <div>
    <p className="text-xs font-medium text-gray-500 dark:text-slate-400">{t.postedBy}</p>
    <p className="text-sm font-semibold text-gray-900 dark:text-slate-100">{sellerProfile?.fullName || listing.userName}</p>
    <p className="text-xs text-gray-400 dark:text-slate-500 mt-0.5">{listing.userEmail}</p>
  </div>
</div>
```

The email is always shown — remove any existing `sellerProfile?.showEmail !== false` check from this block if one exists.

### Part B — Remove "Show Email" toggle from SettingsPage.tsx

In `SettingsPage.tsx`, find the Privacy Settings section. It contains three `<PrivacyRow>` components: Show Email, Show WhatsApp, Show WeChat.

Remove the Show Email row entirely:
```tsx
// DELETE this entire line:
<PrivacyRow label="Show Email" sub="Allow others to see your email address" value={showEmail} onChange={(v) => handlePrivacyToggle("showEmail", v)} />
```

Also remove the `showEmail` state and related code:
1. Remove `const [showEmail, setShowEmail] = useState(true);`
2. Remove `setShowEmail(userProfile.showEmail ?? true);` from the useEffect
3. Remove the `showEmail` branch from `handlePrivacyToggle`:
   Change the function to only handle `showWhatsApp` and `showWeChat`:
```ts
   const handlePrivacyToggle = async (field: "showWhatsApp" | "showWeChat", value: boolean) => {
     if (field === "showWhatsApp") setShowWhatsApp(value);
     else setShowWeChat(value);
     try {
       await updateProfile(user.uid, { [field]: value });
       await refetchProfile().catch(() => {});
     } catch {
       if (field === "showWhatsApp") setShowWhatsApp(!value);
       else setShowWeChat(!value);
     }
   };
```
4. Update all three `<PrivacyRow onChange>` calls that referenced `"showEmail"` — those no longer exist. The remaining two rows call `handlePrivacyToggle("showWhatsApp", v)` and `handlePrivacyToggle("showWeChat", v)`.

### Part C — Remove showEmail from userProfile.ts (optional cleanup)
In `src/lib/userProfile.ts`, find the `updateProfile` function. The `showEmail` field in UserProfile type can remain for backwards compatibility — do NOT remove it from types.ts. Just stop writing it from SettingsPage. No changes needed to userProfile.ts or types.ts.

---

## IMPORTANT CONSTRAINTS
1. Do NOT change firebase.ts.
2. Do NOT add any new npm packages.
3. Do NOT modify Layout.tsx, ListingCard.tsx, HomePage.tsx, AuthModal.tsx, or any lib files except i18n.ts.
4. The WhatsApp `maxLength={20}` with `.slice(0, 20)` in onChange is intentional — international numbers with country code and spaces are typically 12–15 characters, 20 is a safe upper bound.
5. The email shown on ListingDetailPage comes from `listing.userEmail` which is always stored in the Firestore document — it is always available without any profile fetch.
6. After all changes, confirm exactly which files were modified and list the specific sections changed in each file.