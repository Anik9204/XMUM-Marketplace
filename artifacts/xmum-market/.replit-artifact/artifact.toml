kind = "web"
previewPath = "/"
title = "XMUM Market"
version = "1.0.0"
id = "artifacts/xmum-market"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23007

[services.development]
run = "pnpm --filter @workspace/xmum-market run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/xmum-market", "run", "build" ]
publicDir = "artifacts/xmum-market/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23007"
BASE_PATH = "/"
